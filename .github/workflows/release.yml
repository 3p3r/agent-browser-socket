name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            binary: oatmeal-linux-x64
          - name: Linux ARM64
            os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            binary: oatmeal-linux-arm64
          - name: Windows x64
            os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: oatmeal-windows.exe
          - name: macOS x64
            os: macos-latest
            target: x86_64-apple-darwin
            binary: oatmeal-darwin-x64
          - name: macOS ARM64
            os: macos-latest
            target: aarch64-apple-darwin
            binary: oatmeal-darwin-arm64

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install JS dependencies
        run: npm ci --no-audit --no-fund

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Ensure target is installed
        run: rustup target add ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2

      - name: Install Linux build dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pkg-config \
            libx11-dev \
            libxrandr-dev \
            libxtst-dev \
            libxfixes-dev \
            libxinerama-dev \
            libxcursor-dev \
            libxi-dev \
            libxext-dev \
            libxcb1-dev \
            libwayland-dev \
            libxkbcommon-dev \
            libdbus-1-dev \
            libudev-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            libxdo-dev

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package binary (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir -p artifacts
          cp target/${{ matrix.target }}/release/oatmeal artifacts/${{ matrix.binary }}
          chmod +x artifacts/${{ matrix.binary }}

      - name: Package binary (Windows)
        if: runner.os == 'Windows'
        run: |
          New-Item -ItemType Directory -Force -Path artifacts | Out-Null
          Copy-Item target/${{ matrix.target }}/release/oatmeal.exe artifacts/${{ matrix.binary }}
        shell: pwsh

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}
          path: artifacts/${{ matrix.binary }}
          retention-days: 7

  release:
    name: Merge Release Binaries
    needs: build
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: List downloaded artifacts
        run: |
          find artifacts -type f | sort

      - name: Create macOS universal binary
        run: |
          mkdir -p bin
          lipo -create \
            artifacts/oatmeal-darwin-x64/oatmeal-darwin-x64 \
            artifacts/oatmeal-darwin-arm64/oatmeal-darwin-arm64 \
            -output bin/oatmeal-mac
          chmod +x bin/oatmeal-mac
          lipo -info bin/oatmeal-mac

      - name: Create Linux self-extracting wrapper
        run: |
          mkdir -p /tmp/oatmeal-linux-payload
          cp artifacts/oatmeal-linux-x64/oatmeal-linux-x64 /tmp/oatmeal-linux-payload/
          cp artifacts/oatmeal-linux-arm64/oatmeal-linux-arm64 /tmp/oatmeal-linux-payload/
          chmod +x /tmp/oatmeal-linux-payload/oatmeal-linux-x64
          chmod +x /tmp/oatmeal-linux-payload/oatmeal-linux-arm64

          cat > bin/oatmeal-linux <<'WRAPPER'
          #!/usr/bin/env sh
          set -eu

          ARCH="$(uname -m)"
          case "$ARCH" in
            x86_64|amd64)
              BIN="oatmeal-linux-x64"
              ;;
            aarch64|arm64)
              BIN="oatmeal-linux-arm64"
              ;;
            *)
              echo "Unsupported architecture: $ARCH" >&2
              exit 1
              ;;
          esac

          SELF="$0"
          PAYLOAD_LINE="$(awk '/^__OATMEAL_LINUX_PAYLOAD__$/ { print NR + 1; exit }' "$SELF")"
          if [ -z "${PAYLOAD_LINE:-}" ]; then
            echo "Corrupt launcher: missing payload marker" >&2
            exit 1
          fi

          TMPDIR="$(mktemp -d "${TMPDIR:-/tmp}/oatmeal-linux.XXXXXX")"
          trap 'rm -rf "$TMPDIR"' EXIT INT TERM

          tail -n +"$PAYLOAD_LINE" "$SELF" | tar -xzf - -C "$TMPDIR"
          chmod +x "$TMPDIR/$BIN"
          exec "$TMPDIR/$BIN" "$@"
          __OATMEAL_LINUX_PAYLOAD__
          WRAPPER

          tar -czf /tmp/oatmeal-linux-payload.tgz -C /tmp/oatmeal-linux-payload .
          cat /tmp/oatmeal-linux-payload.tgz >> bin/oatmeal-linux
          chmod +x bin/oatmeal-linux
          rm -f /tmp/oatmeal-linux-payload.tgz

      - name: Finalize Windows binary
        run: |
          cp artifacts/oatmeal-windows.exe/oatmeal-windows.exe \
            bin/oatmeal-windows.exe

      - name: Verify release binaries
        run: |
          ls -la bin/
          test -f bin/oatmeal-mac
          test -f bin/oatmeal-linux
          test -f bin/oatmeal-windows.exe
          grep -q '^__OATMEAL_LINUX_PAYLOAD__$' bin/oatmeal-linux

      - name: Upload universal binaries for testing
        uses: actions/upload-artifact@v4
        with:
          name: universal-binaries
          path: |
            bin/oatmeal-mac
            bin/oatmeal-linux
            bin/oatmeal-windows.exe
          retention-days: 7

  test-binaries:
    name: Test ${{ matrix.name }}
    needs: release
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x64
            os: ubuntu-22.04
            binary: oatmeal-linux
          - name: Linux ARM64
            os: ubuntu-22.04-arm
            binary: oatmeal-linux
          - name: macOS x64
            os: macos-latest
            binary: oatmeal-mac
          - name: macOS ARM64
            os: macos-latest
            binary: oatmeal-mac
          - name: Windows x64
            os: windows-latest
            binary: oatmeal-windows.exe

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install JS dependencies
        run: npm ci --no-audit --no-fund

      - uses: actions/download-artifact@v4
        with:
          name: universal-binaries
          path: bin/

      - name: Install Linux runtime dependencies
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-0 libxdo3

      - name: Make binary executable
        if: runner.os != 'Windows'
        run: chmod +x bin/${{ matrix.binary }}

      - name: Run binary smoke tests
        run: npm run test:binary -- bin/${{ matrix.binary }}

  publish-nightly:
    name: Publish Nightly Draft Release
    needs:
      - release
      - test-binaries
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: universal-binaries
          path: bin/

      - name: Upsert draft release
        run: |
          TAG="nightly"
          if gh release view "$TAG" &>/dev/null; then
            gh release delete "$TAG" --yes
          fi
          gh release create "$TAG" \
            --title "Nightly (draft)" \
            --draft \
            --prerelease \
            --notes "Nightly auto-build from \`main\` (${GITHUB_SHA::7})" \
            bin/oatmeal-mac \
            bin/oatmeal-linux \
            bin/oatmeal-windows.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-pages:
    name: Build Pages
    needs: publish-nightly
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install UI dependencies
        working-directory: ui
        run: npm ci --no-audit --no-fund

      - name: Build combined site
        working-directory: ui
        run: npm run build:pages

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ui/dist

  deploy-pages:
    name: Deploy to GitHub Pages
    needs: build-pages
    runs-on: ubuntu-22.04
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
